# Full dependencies (with packages that may require compilation)
# Use this after installing build tools or in Linux/Mac

# Transformers (requires Rust tokenizers)
transformers>=4.35.0
tokenizers>=0.13.0
sentence-transformers>=2.2.2

# Audio VAD (requires C compilation)
webrtcvad>=2.0.10

# NLP (

requires compilation)
spacy>=3.7.0

# Anthropic (requires Rust)
anthropic>=0.8.0

# Audio noise reduction
noisereduce>=3.0.0

# Install
:
# pip install -r requirements.txt
# pip install -r requirements-full.txt
